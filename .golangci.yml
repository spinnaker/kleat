run:
  skip-dirs:
    - api/client
  modules-download-mode: readonly
issues:
  exclude-use-default: false
linters:
  enable:
    - gofmt
    - goimports
    - golint
    - govet
goimports:
  local-prefixes: github.com/spinnaker/kleat
